# Starter pipeline

pool:
  vmImage: 'Ubuntu 18.04'
  environment: nice-vm 


steps:
- task: CmdLine@2
  inputs:
    script: |
      ssh sudipta-ubuntu@13.82.82.32
      echo Sudiepta
      sudo apt install mosquitto
      #pwd
      sudo cp ~/nice-mosquitto.conf /etc/mosquitto/conf.d
      sudo systemctl restart mosquitto.service
      



